[project]
name = "portta-multiviewer-control"
version = "0.1.0"
description = "Utility for controlling PORTTA 4x2 multiviewer via its USB CDC RS232 serial protocol"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyserial>=3.5",
]
license = "0BSD"
